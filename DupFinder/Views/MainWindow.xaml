<Window x:Class="DupFinder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DupFinder"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10 10 10 10"/>
            <Setter Property="Margin" Value="10 10 10 10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="MinWidth" Value="150" />
        </Style>
    </Window.Resources>
    <StackPanel>
        
        <Label FontSize="40">DupFinder!</Label>

        <StackPanel Orientation="Horizontal">
            <Button x:Name="button_FolderPicker" Command="{Binding Path=ChooseDirectoryCommand}">Choose Directory</Button>
            <Label x:Name="label_ChosenFolder" VerticalAlignment="Center">Chosen folder:</Label>
            <Label x:Name="chosenFolder" VerticalAlignment="Center" Content="{Binding Path=SelectedPath}"></Label>
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <Button x:Name="button_LoadImages" Command="{Binding Path=LoadImagesIntoMemoryCommand}">Load Images</Button>
            <Label x:Name="label_loadedImages" VerticalAlignment="Center" Content="Loaded Images:"></Label>
            <Label x:Name="loadedImages" VerticalAlignment="Center" Content="{Binding Path=LoadedImages}"></Label>
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <Button x:Name="button_ProcessImages" Content="Process Images" Command="{Binding Path=FindSimilarImagesCommand, UpdateSourceTrigger=PropertyChanged}"/>
            <Label x:Name="label_processedImages" VerticalAlignment="Center">Processed images:</Label>
            <Label x:Name="processesdImages" VerticalAlignment="Center" Content="{Binding Path=SimilarImages}"></Label>
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <Button x:Name="button_MoveImages" Content="Move Images" Command="{Binding Path=MoveImagesCommand}"/>
            <Label x:Name="label_movedImages" VerticalAlignment="Center">X images moved (not implemented)</Label>
        </StackPanel>

        <Button Content="Options" Command="{Binding Path=ShowOptionsCommand}"></Button>
    </StackPanel>
</Window>
