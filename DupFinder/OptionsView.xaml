<Window x:Class="DupFinderApp.OptionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DupFinderApp"
        mc:Ignorable="d"
        Title="OptionsView" Height="450" Width="800">
    <StackPanel>
        <CheckBox IsChecked="{Binding Path=CheckPixels, Mode=TwoWay}">Pixel count</CheckBox>
        <CheckBox IsChecked="{Binding Path=CheckDates, Mode=TwoWay}">Date</CheckBox>
        <CheckBox IsChecked="{Binding Path=CheckSize, Mode=TwoWay}">Filesize</CheckBox>
    </StackPanel>
</Window>
